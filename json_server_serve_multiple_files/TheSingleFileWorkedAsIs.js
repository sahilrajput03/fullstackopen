module.exports = function() {
    return {
      persons: require('./persons.json'),
      two: require('./db_2.json')
    }
  }
  // person(array_of_objects was actually contained in db.json file)